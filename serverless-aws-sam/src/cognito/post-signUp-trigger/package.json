{
  "name": "signup-savedata",
  "version": "1.0.0",
  "description": "A lambda function to save data initial user data to dynamodb",
  "main": "saveUserData.js",
  "type": "module",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.598.0",
    "@aws-sdk/lib-dynamodb": "^3.598.0",
    "esbuild": "^0.14.54"
  },
  "devDependencies": {
    "@types/node": "^20.14.4",
    "typescript": "^5.5.3"
  }
}
