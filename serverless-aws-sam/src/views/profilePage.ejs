<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="<%= staticContentUrl %>/ejs/styles.css" />
    <script defer src="<%= staticContentUrl %>/ejs/runtime.js"></script>
    <script defer src="<%= staticContentUrl %>/ejs/navBar.js"></script>
    <script defer src="<%= staticContentUrl %>/ejs/profilePage.js"></script>
    <title>Document</title>
  </head>
  <body>
    <body class="min-h-screen bg-base-300 roboto antialiased">
      <%- include('./components/navBar', { profileColor, username,
      navJoinRequest, isProduction, staticContentUrl, domainUrl}); %>
      <div
        class="container mx-auto bg-base-100 rounded-lg text-base-content p-2"
      >
        <div class="flex flex-col items-center gap-4">
          <h1 class="text-5xl mdsm:text-6xl text-center">Profile Page</h1>
          <div class="space-y-2">
            <h2 class="text-4xl text-center">User Info</h2>
            <div class="text-lg mdsm:text-2xl text-center space-y-2">
              <div>User Name: <%= username %></div>
              <div>Email: <%= email %></div>
              <div>Profile Color: <%= profileColor %></div>
            </div>
          </div>
        </div>
        <div class="text-lg mdsm:text-xl flex justify-end">
          <button
            id="DELETE_ACCOUNT_BUTTON"
            onclick="Delete_Account_Model.showModal()"
            class="bg-error text-error-content text-lg mdsm:text-xl rounded-lg font-bold p-1 mdsm:p-2 mt-2"
          >
            Delete Account
          </button>
          <dialog
            id="Delete_Account_Model"
            class="modal modal-bottom sm:modal-middle"
          >
            <div class="modal-box flex flex-col gap-4">
              <form method="dialog">
                <div class="flex items-center">
                  <button
                    class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2"
                  >
                    ✕
                  </button>
                </div>
              </form>
              <p class="text-lg">
                Press ESC key or click the button below to close
              </p>
              <div>
                <p>Are You sure you want to delete Your Account?</p>
                <p>
                  Enter <span class="font-bold">Delete</span> to delete your
                  account
                </p>
              </div>
              <input
                id="DELETE_ACCOUNT_INPUT"
                type="text"
                placeholder="Type here"
                class="input input-bordered w-full"
              />
              <div class="flex justify-end">
                <button
                  id="DELETE_ACCOUNT_CONFIRM_BUTTON"
                  class="btn btn-error text-error-content text-lg w-1/3"
                >
                  Confirm
                </button>
              </div>
            </div>
            <form method="dialog" class="modal-backdrop">
              <button>Close</button>
            </form>
          </dialog>
        </div>
      </div>
    </body>
  </body>
</html>
