{
  "main": "joinRoom.ts",
  "description": "The lambda joinRoom route for api gateway's websocket api for when a user wants to join a room. Route key is joinroom",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/aws-lambda": "^8.10.142",
    "@types/node": "^20.14.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
