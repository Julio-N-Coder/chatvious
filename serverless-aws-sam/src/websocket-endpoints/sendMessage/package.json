{
  "main": "sendMessage.ts",
  "description": "The lambda sendMessage route for api gateway's websocket api for when a user sends a message to a room. The Route key is sendmessage",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.623.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/aws-lambda": "^8.10.142",
    "@types/node": "^20.14.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
